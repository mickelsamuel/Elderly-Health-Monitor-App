<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <!-- Vertical LinearLayout to arrange child views -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- TextView for displaying patient name -->
        <TextView
            android:id="@+id/patientNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Patient Name"
            android:textSize="18sp"/>

        <!-- TextView for displaying patient ID -->
        <TextView
            android:id="@+id/patientIDTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Patient ID"
            android:textSize="18sp"/>

        <!-- TextView for displaying patient's heart rate -->
        <TextView
            android:id="@+id/patientHeartRateText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Heart Rate: 98"
            android:textSize="18sp"/>

        <!-- TextView for displaying patient's temperature -->
        <TextView
            android:id="@+id/patientTemperatureText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Temperature: 36.5°C"
            android:textSize="18sp"/>

        <!-- TextView for displaying accelerometer data -->
        <TextView
            android:id="@+id/patientAccelerometerText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Accelerometer: X: 0.1, Y: 0.2, Z: 9.8"
            android:textSize="18sp"/>

        <!-- Horizontal LinearLayout for action buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:paddingTop="8dp">

            <!-- Button to view more patient information -->
            <Button
                android:id="@+id/infoPatientButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="INFO"
                android:layout_marginEnd="8dp"/>

            <!-- Button to remove patient from the list -->
            <Button
                android:id="@+id/removePatientButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="REMOVE"/>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
